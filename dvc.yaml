stages:

  download_data:
    cmd: python src/data/download_data.py
    params:
    - kaggle_competition
    outs:
    - data/raw/sample_submission.csv:
        cache: false
    - data/raw/test.csv:
        cache: false
    - data/raw/train.csv:
        cache: false

  dfs:
    cmd: python src/feature/dfs.py
    params:
      - features
      - max_depth
      - transform_primitives
    deps:
      - data/raw/train.csv
      - data/raw/test.csv
    outs:
      - data/interim/train_augmented.csv:
          cache: false
      - data/interim/test_augmented.csv:
          cache: false

  pca:
    cmd: python src/feature/pca.py
    params:
      - n_components
    deps:
      - data/interim/train_augmented.csv
    outs:
      - data/interim/train_pca.csv:
          cache: false

  lgbm_modelling:
    cmd: python src/model/lgbm.py
    params:
      - features
      - n_components
    deps:
      - data/interim/train_pca.csv
    outs:
      - models/lgbm/model.txt

#  lgbm_inference:
#    cmd: python src/inference/lgbm_inf.py
#    params:
#      - features
#    deps:
#      - models/lgbm/model.txt
#      - data/interim/test_scaled.csv
#    outs:
#      - data/submission/submission.csv

# You may submit a maximum of 5 entries per day.
#  submission:
#    cmd: kaggle competitions submit
#      tabular-playground-series-jan-2021
#      -f data/submission/submission.csv
#      -m "LightGBM"
